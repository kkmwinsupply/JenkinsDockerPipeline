pipeline {
    agent none
    stages {
        stage('Build') {
            steps {
                node {
                    sh 'node --version'
                    sh 'pwd'
                }
            }
        }
    }
}
